{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "tbuild",
            "type": "shell",
            "command": "clear && g++ -o strmang -std=c++11 `wx-config --cxxflags` main.cpp FB_SM.cpp `wx-config --libs` ",
            
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}